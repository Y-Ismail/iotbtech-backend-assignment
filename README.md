# KEY TAkEAWAY

## CLASS 31  
node has a huge ecosystem and bun is new but faster, Buffer is raw binary bytes the computer understands. Instead of buffering whole file use ReadStream to process the data piece by piece so that RAM usage stays flat no matter how large the file is

## CLASS 32
Express is a thin layer built on node that makes routing declarative and simple. Route tells where the request goes, controller manages what should happen with req and response, and service gives the business logic to be performed


## CLASS 33
Express is a pipeline of middleware, every request flows through a pipeline of middleware. Middleware runs in registration order. In logging, console is for you now while File is permanent record for future use. Good backends treat errors as a first class pipeline stage, they fail fast, fail loud and fail clean.

